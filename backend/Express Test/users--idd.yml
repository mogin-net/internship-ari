info:
  name: /users/:idd
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/users/:id
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
